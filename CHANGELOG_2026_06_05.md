# Changelog — June 5, 2026

## 1. Bot-to-Bot Whitelisting & Integration Verification
* **Bot Whitelisting Bypass:** Extracted `MAIN_BOT_USER_ID` and `OPS_BOT_USER_ID` in [config.py](file:///Users/terminal/houseops/app/config.py#L45-L55) to recognize `@DQBotOpsBot` traffic. Modified `telegram_webhook` in [main.py](file:///Users/terminal/houseops/main.py#L174-L215) to dynamically authenticate inbound webhook messages originating from `@DQBotOpsBot`'s ID, whitelisting it as a `tier2` role.
* **Egress & Ingress Loopback Check:** Integrated a loopback ping check in [get_ops_status_report](file:///Users/terminal/houseops/app/ops_bot.py#L198-L266) inside [ops_bot.py](file:///Users/terminal/houseops/app/ops_bot.py). When compiling the status report, it sends a signed `ping_test` request to the main bot's webhook. The main bot receives this, intercepts the request, and dispatches a confirmation of egress success (`"Main Bot Egress Test"`) back to the ops bot channel via `send_ops_message`.
* **Testing:** Added comprehensive integration tests in [test_main.py](file:///Users/terminal/houseops/tests/test_main.py#L276-L320) (`test_telegram_webhook_ops_bot_ping_test`) and [test_ops_bot.py](file:///Users/terminal/houseops/tests/test_ops_bot.py#L228-L262) (`test_get_ops_status_report_bot_integration_success`) verifying webhook routing, signature matching, and mock egress status responses.

## 2. Refactoring & Performance Optimizations
* **Async Threadpool Delegation:** Wrapped CPU-bound and synchronous blocking operations in [main.py](file:///Users/terminal/houseops/main.py) with FastAPI's `run_in_threadpool`. This ensures third-party operations (Firestore DB streams/gets, HTTP queries, cloud task scheduling, and local/Vertex AI token counts) do not block the central asynchronous event loop.
* **Firestore Read Optimization:** Consolidated document read logic in `update_task_status` in [tools_module2.py](file:///Users/terminal/houseops/app/tools_module2.py#L120-L208). The permission check is now performed atomically *inside* the transaction via `transaction.get(task_ref)` instead of running a separate read command beforehand. This cuts Firestore read operations in half for task status updates.
* **Firestore Transaction Overhead Removal:** Simplified `update_driver_availability` in [tools_fleet.py](file:///Users/terminal/houseops/app/tools_fleet.py#L434-L457). Removed the transactional wrapper entirely in favor of a single `.set(..., merge=True)` call directly on the document reference, avoiding unnecessary locking and latency.
* **Token Compiling Performance:** Introduced a fast, local character-length heuristic (~4 characters per token) in [history.py](file:///Users/terminal/houseops/app/history.py#L71-L80) to trim old conversations before making expensive calls to Gemini/Vertex AI `count_tokens_text`. This drastically improves response latency during long conversation history compiles.

## 3. Cleanups & Housekeeping
* **Removed QWEN.md:** Deleted the unused, empty `QWEN.md` file from the repository root to keep the workspace clean.
